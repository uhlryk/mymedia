import Extension from "../Extension.main";
export default class AttributeExtension extends Extension {
  constructor(extensionName, ...configurations) {
    super(extensionName, ...configurations);
  }
}
