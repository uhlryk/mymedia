import IFile from "../types/file.interface";
import IResource from "../../../../shared/types/resource.interface";

export default function makeResourceListUnique(fileList: Array<IFile>, resourceList: Array<IResource>) {

    return resourceList;
}
